# Packaged Skill Files

This folder contains zipped skill packages generated from the folders in `skills/`.

Each zip file should contain one skill folder with a `SKILL.md` file and supporting files.

## Regenerate packages

Run:

```bash
bash package-skills.sh
```

## Validate first

Run:

```bash
python scripts/validate-skills.py
```
