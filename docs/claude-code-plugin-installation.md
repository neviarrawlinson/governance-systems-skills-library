# Claude Code Plugin Installation

This repository includes a Claude Code plugin manifest so the Governance Systems Skills Library can be positioned as both:

1. A Claude-compatible GRC skills library
2. A Claude Code plugin-ready Governance Systems Engineering toolkit

## Plugin Structure

The plugin metadata is stored here:

```text
.claude-plugin/plugin.json
